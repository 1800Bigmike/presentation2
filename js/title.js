// custom.js

// Wait for the DOM to be ready before manipulating it
document.addEventListener("DOMContentLoaded", function() {
    // Add the logo dynamically
    const logoContainer = document.getElementById('logo');
    logoContainer.style.backgroundImage = "url('data/oregon-state-beavers-logo@logotyp.us.svg')"; // Replace with the path to your logo
});
